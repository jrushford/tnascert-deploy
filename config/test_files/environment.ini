[deploy_default]
api_key = ${API_KEY}
cert_basename = ${CERT_BASENAME}
client_api = ${CLIENT_API}
private_key_path = ${PRIVATE_KEY_PATH}
full_chain_path = ${FULL_CHAIN_PATH}
connect_host = ${CONNECT_HOST}.${DOMAIN_NAME}
port = ${CONNECT_PORT}
protocol = ${PROTOCOL}
tls_skip_verify = ${TLS_SKIP_VERIFY}
delete_old_certs = ${DELETE_OLD_CERTIFICATES}
add_as_ui_certificate = ${ADD_AS_UI_CERTIFICATE}
add_as_ftp_certificate = ${ADD_AS_FTP_CERTIFICATE}
add_as_app_certificate = ${ADD_AS_APP_CERTIFICATE}
app_list = ${APP_LIST}
timeoutSeconds = ${TIMEOUT_SECONDS}
debug = ${DEBUG}
